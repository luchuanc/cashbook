services:
  main:
    container_name: cashbook4_local
    depends_on: 
      - db
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@cashbook_db_local:5432/cashbook?schema=public"
      NUXT_DATA_PATH: "/app/data"
      NUXT_AUTH_SECRET: "auth_secret" 
      NUXT_ADMIN_USERNAME: "admin"
      NUXT_ADMIN_PASSWORD: "fb35e9343a1c095ce1c1d1eb6973dc570953159441c3ee315ecfefb6ed05f4cc"
    ports:
      - 9090:9090
  db:
    container_name: cashbook_db_local
    image: postgres:17.4-alpine3.21
    restart: always
    shm_size: 128mb
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cashbook
